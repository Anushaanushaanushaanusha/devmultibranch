pipeline {
    agent any

    stages {
     stage("hotfix branch") {
   steps {
    sh 'echo "this is hotfix branch"'
    }
   }
    stage("hotfix") {
  steps {
   sh 'echo "hotfix application.."'
 }
}
  stage("deploy application") {
    steps {
    sh 'echo "deploying application..."'
	}
      }
   }
}
